# Plan: US-10 · Escalamiento preciso con árbol de decisión

## Entendimiento del issue

**Objetivo**: Implementar un sistema de escalamiento basado en árbol de decisión que:
- Cubra los 5 `intent_type` de US-05: `FAQ`, `product_info`, `escalate_quote`, `escalate_technical`, `escalate_order`
- Tasa de falsos escalamientos (escala cuando no debería) < 15%
- Tasa de falsos no-escalamientos (no escala cuando debería) < 10%

**Dependencias**: US-05 (#19) - clasificación automática de intent_type ✓ implementada

---

## Estado actual

1. **Clasificación intent_type** (US-05): El LLM clasifica usando markers `[INTENT: <tipo>]`
2. **Lógica de escalamiento**: Simple lista de tipos que escalan:
   ```python
   escalate_intents = ["escalate_quote", "escalate_technical", "escalate_order"]
   should_escalate = intent_type in escalate_intents
   ```
3. **Detector de escalamiento legacy**: `rag/escalation.py` usa keywords (no usado actualmente)

---

## Análisis de gaps

| Criterio | Estado actual | Requerido |
|----------|---------------|-----------|
| Árbol de decisión | ❌ Lista simple | ✓ Implementar |
| Confianza del LLM | ❌ No se captura | ✓ Agregar |
| Thresholds ajustables | ❌ Hardcodeado | ✓ Configurable |
| Métricas medibles | ✓ harness dataset | <15% fp, <10% fn |

---

## Plan de implementación

### Fase 1: Estructura del árbol de decisión

**Archivo**: `backend/app/escalation_tree.py` (nuevo)

```python
class EscalationDecisionTree:
    """Árbol de decisión para escalamiento preciso."""
    
    # Nodos de decisión por intent_type
    DECISION_RULES = {
        "FAQ": DecisionNode(action=Action.NO_ESCALATE, confidence_boost=0.9),
        "product_info": DecisionNode(
            action=Action.CHECK_COMPLEXITY,
            # Si consulta = specs simples → no escalar
            # Si consulta = compara múltiples → escalar
        ),
        "escalate_quote": DecisionNode(action=Action.ESCALATE, confidence_boost=0.95),
        "escalate_technical": DecisionNode(action=Action.ESCALATE, confidence_boost=0.95),
        "escalate_order": DecisionNode(action=Action.ESCALATE, confidence_boost=0.95),
    }
```

### Fase 2: Integración de confianza del LLM

1. **Modificar `ARTE_SYSTEM_PROMPT`** para solicitar confidence score:
   ```
   Al final de cada respuesta incluye: [CONFIDENCE: 0.XX]
   ```

2. **Actualizar `_extract_intent_type`** para capturar confidence:
   ```python
   INTENT_CONFIDENCE_RE = re.compile(r"\[CONFIDENCE:\s*(0?\.\d+)\]")
   ```

### Fase 3: Lógica de umbrales

**Archivo**: `backend/app/config.py` (extender settings)
```python
class Settings:
    # Thresholds de escalamiento
    ESCALATION_CONFIDENCE_THRESHOLD: float = 0.7  # Default
    FALSE_POSITIVE_LIMIT: float = 0.15  # 15%
    FALSE_NEGATIVE_LIMIT: float = 0.10  # 10%
```

### Fase 4: Evaluación y ajuste

1. **Extender `evaluation/harness/dataset.json`** para incluir casos grises:
   - "Tengo una duda técnica sobre inversores" → ambiguous
   - "Quiero comparar 3 modelos de paneles" → product_info + complexity

2. **Script de medición** `evaluation/escalation_eval/run_eval.py`:
   - Calcular false positives (escaló sin necesidad)
   - Calcular false negatives (no escaló cuando debía)
   - Ajustar thresholds hasta cumplir <15% FP y <10% FN

---

## Archivos a modificar/crear

| Archivo | Acción | Descripción |
|---------|--------|-------------|
| `backend/app/escalation_tree.py` | Crear | Árbol de decisión con nodos |
| `backend/app/llm_client.py` | Modificar | Agregar confidence al prompt |
| `backend/main.py` | Modificar | Usar `EscalationDecisionTree` |
| `backend/app/config.py` | Modificar | Agregar thresholds |
| `evaluation/escalation_eval/dataset.json` | Crear | Dataset específico |
| `evaluation/escalation_eval/run_eval.py` | Crear | Script de evaluación |

---

## Criterios de aceptación

- [ ] Árbol de decisión cubre los 5 intent_type
- [ ] False escalation < 15% sobre harness dataset
- [ ] False non-escalation < 10% sobre harness dataset
- [ ] Tests unitarios para `EscalationDecisionTree`
- [ ] Tests de integración con el evaluation harness